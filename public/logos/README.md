# Logos

| Файл | Где | Формат | Заметки |
|---|---|---|---|
| `gosuslugi-dom.png` | AppHeader (левый край) | **PNG** 113×40 | В Figma логотип "Госуслуги Дом" состоит из 9 отдельных vector-path'ов; unified SVG не было — приходится использовать растровый рендер. Для retina-качества заменить на SVG через Figma desktop (правый клик по `node-id=1-27064` → Export → SVG). |
| `partner-minstroy.svg` | Footer | SVG | Минстрой России |
| `partner-mincifry.svg` | Footer | SVG | Минцифры |
| `partner-ois.svg` | Footer | SVG | ОИС |

## Чего не хватает

- **SVG-версия логотипа "Госуслуги Дом"** — нужно экспортировать unified SVG из Figma (через playwright или вручную).
- **Логотип "ГИС ЖКХ" + герб** — в шапке используется парный лого (логотип "Госуслуги Дом" + разделитель + герб + "Государственная информационная система ЖКХ"). Герб не был отдельным instance в прототипе. См. `refs/components/header.png` как rendered reference.
- **Логотип "Госуслуги" (партнёрский, текстовый)** — в footer есть, но в прототипе как отдельный instance его не нашёл. Скорее всего это часть `partner-минстрой/-минцифры` композиции или отдельный узел вне MCP-выборки.
